server {
  listen 80;

  server_name 40.78.100.134;

  location / {
    proxy_pass http://web:3000;
    proxy_connect_timeout 5s;
    proxy_read_timeout 10s;
  }

  location /api {
    proxy_pass http://api:3001;
    rewrite ^/api/(.*) /$1 break;
  }
}